export const AuthPatterns = {
  LOGIN: 'auth.login',
  NEW_USER: 'auth.user.create',
  FIND_USER: 'auth.user.find',
  ALL_USERS: 'auth.user.findAll',
  DEACTIVATE_USER: 'auth.user.deactivate',
  UPDATE_USER: 'auth.user.update',
} as const;
